{
	"name": "video-ad-network",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"dev": "turbo dev",
		"build": "turbo build",
		"lint": "turbo lint",
		"format": "turbo format",
		"test": "turbo test",
		"deploy": "run-s deploy:*",
		"deploy:ad-server": "pnpm run --filter @video-ad-network/ad-server deploy",
		"deploy:tracker": "fly deploy -c infra/flyio/tracker/fly.toml --dockerfile infra/flyio/tracker/Dockerfile",
		"dev:start": "concurrently --kill-others-on-fail \"nohup turso dev --db-file=./data/dev.db　> turso-dev.log 2>&1 &\"",
		"dev:stop": "pkill -f 'turso dev' || true",
		"dev:logs": "tail -f turso-dev.log",
		"db:seed": "pnpm run --filter @video-ad-network/db db:seed"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "catalog:",
		"npm-run-all": "catalog:",
		"turbo": "catalog:"
	},
	"packageManager": "pnpm@9.12.3"
}
